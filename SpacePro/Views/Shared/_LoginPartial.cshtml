@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li><a id="registerLink" href="/Account/Register" style="margin-top:20px" class="btn btn-dark">Register</a></li>
        <li style="width:120px"></li>
        @*<li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>*@
        <li><a id="loginLink" href="/Account/Login" style="margin-top:20px" class="btn btn-dark">Log in</a></li>
    </ul>
}
